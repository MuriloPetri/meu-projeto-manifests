# deployment.yaml
apiVersion: apps/v1
kind: Deployment
metadata:
  name: meu-projeto-app
spec:
  replicas: 1
  selector:
    matchLabels:
      app: meu-projeto
  template:
    metadata:
      labels:
        app: meu-projeto
    spec:
      containers:
      - name: meu-projeto-container
        # Esta linha será atualizada automaticamente pelo seu CI/CD
        image: muliropetri123/hello-app:f78cc891a76587d9cc2f33285968a3bc403124ba
        ports:
        - containerPort: 80